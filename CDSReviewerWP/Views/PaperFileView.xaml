<UserControl x:Class="CDSReviewerWP.Views.PaperFileView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">
    
    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">
    	<StackPanel>
    		<TextBlock x:Name="FileName" TextWrapping="Wrap"/>
    		<TextBlock x:Name="FIleDate" TextWrapping="Wrap"/>
    		<StackPanel Orientation="Horizontal">
    			<TextBlock TextWrapping="Wrap" Text="Version" VerticalAlignment="Top"/>
    			<TextBlock x:Name="Version" TextWrapping="Wrap" Margin="5,0,0,0"/>
    		</StackPanel>
    	</StackPanel>

    </Grid>
</UserControl>